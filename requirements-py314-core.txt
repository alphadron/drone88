# FacilityPath 핵심 의존성 (verify.yml CI에서 사용) — (주)드론아이디 AI연구소 · DID-DP-2026-0829
# 코어 파이프라인(io_model/io_flightpath -> surface_analyzer -> camera ->
# planner_slope/path_adapter(+tilt_solver) -> planner_base -> kml_writer)에
# 필요한 최소 의존성만 포함. 3D뷰(pyvista)·UI(streamlit)는 포함하지 않는다.
numpy==2.4.6          # 좌표·행렬 연산 전반
scipy==1.17.1          # cKDTree — path_adapter 최근접점 탐색
pyproj==3.7.2          # AEQD 지역투영 <-> WGS84 변환 (kml_writer)
matplotlib==3.11.1     # verify_*.py 3D 시각화 출력(png)
simplekml==1.3.6       # Google Earth KML 작성 (kml_writer)
trimesh==5.1.0         # OBJ/PLY/STL 메시 입력 (io_model)
